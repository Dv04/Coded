JSON stands for JavaScript Object Notation.
it is a text format for storing and transporting data.
JSON is self-describing and easy to understand.

JSON format is syntactically similar to the code for creating HS objects, thus, it can easily convert JSON data for javascript.

Since the format is text only, it can be sent easily between computers and can be used by any programming language.