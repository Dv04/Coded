\documentclass[varwidth=4.8cm]{standalone}

%\usepackage{verbatim}
\usepackage{minted}

\begin{document}

\begin{minted}{java}
while (true) {
	A a = read(ch0);
	B b = read(ch1);
	C c = read(ch2);
	D d = op(a, b, c);
	emit(d);
}
\end{minted}

\end{document}
