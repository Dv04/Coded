# Unnecessary Items (none remaining)

All previously identified extras (AMP, compile, determinism toggles, synthetic fallbacks, oversized sweeps, etc.) have been removed. No additional non-rubric features remain in the active codebase.
